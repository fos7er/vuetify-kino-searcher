const state = {
  isAuthorized: false,
}

const getters = {}

const mutations = {
  login() {
    console.log('login')
  },
}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
